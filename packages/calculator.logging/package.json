{
  "name": "@acme/calculator.logging",
  "description": "A simple calculator with logging.",
  "version": "0.0.0",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib/**/*"
  ],
  "license": "UNLICENSED",
  "scripts": {
    "build": "run-s ~clean ~compile",
    "prepublishOnly": "run-s ~test",
    "test": "run-s ~test",
    "~clean": "shx rm -rf lib",
    "~compile": "tsc",
    "~test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "@acme/calculator": "0.0.0",
    "@types/jest": "^25.2.3",
    "jest": "^26.0.1",
    "npm-run-all": "^4.1.5",
    "shx": "^0.3.2",
    "ts-jest": "^26.0.0",
    "typescript": "^3.9.3"
  }
}
